# Standalone qualification CI

The owner approved the exact `standalone-ci-proposal.md` and adjacent patch on 11 September 2026, then pre-approved configuration changes explicitly specified in the implementation plan. This record covers the first local WP4 slice of the accepted extraction plan, sections 8.4-8.5; it does not imply authorization to commit, push or publish.

The qualification workflow now resolves package commands and reports at the standalone root, without ONI scope selection. The six Windows/Linux runtime entries, two Linux mutation profiles, strict dependency review, pinned actions and failure propagation are preserved. Node 24 entries run the actual development setup; other supported runtimes use explicit locked installs. Setup-node caching is disabled explicitly. The aggregate is named `BBCode / Qualification`.

The migrated MIT workflow regression runs under the existing Node test runner and strict lint/type checks. Its original notice is retained. It uses the existing YAML parser, now declared directly as development dependency 2.9.0; the lock changed only its root declaration, with no resolution/integrity changes. The obsolete Jest-dependent test was retired after its unchanged import blob and passing replacement were verified.

Evidence in `artifacts/portability/` includes 16 passing preservation tests in `ci-preservation.log`, two intended standalone-contract failures in `ci-red.log`, and 18 passing workflow cases in `ci-green.log`. The real Bash aggregate accepts all-success and rejects all twelve unsuccessful or absent prerequisite combinations. The initial full check exposed missing native type narrowing in the test; that failure is retained in `ci-check.log`. The correction passes the full package check in `ci-check-final.log`, including 685 tests, unchanged runtime coverage thresholds and strict 100% type coverage (18,345 references plus 305 API-docs references).

Independent review confirmed exact semantic equality with the approved workflow proposal, unchanged resolved dependencies, licence preservation, test discovery and native type checking. Local workflow parsing and shell tests do not prove hosted matrix execution, mutation scores or branch protection. Destination SDLC/governance, hosted qualification and release identity remain subsequent migration work. Source ONI content and runtime converter behavior are unchanged.

The independent checkout with spaces in its path passed native `setup:development` and the complete `check` sequence for the copied 204-file snapshot. Its logs are retained as `artifacts/portability/ci-setup.log` and `ci-check.log`, with copied-source hashes in the destination's `ci-spaced-inputs.json`. Native library Stryker `--dryRunOnly` also passed (`ci-mutation-dry-run.log`), executing 43 test files in its isolated instrumented sandbox without testing mutants; this is sandbox compatibility evidence, not a new mutation score. All work remains uncommitted and no remote configuration or publication was performed.
