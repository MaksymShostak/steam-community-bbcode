# Standalone reuse and rights assessment

The accepted extraction requires continuity of the existing SDLC.
Reusing its tested Python engine and native setup transaction avoids a new state engine or configuration writer.
The maintained jsonschema/PyYAML parsers retain validation ownership; npm remains the package command interface. node:test replaces only the imported Jest workflow regression, preserving real shell behavior.

The source package has no supported updater: use its reviewed selective-copy procedure and retain pre-adaptation source hashes in .sdlc/UPSTREAM.json.
The residual custom work is destination verification, governance, source identity and activation evidence.
No ONI routing or mod build is needed.

The four direct Python selections are jsonschema4.26.0 (MIT), PyYAML6.0.3 (MIT), rfc3339-validator0.1.4 (MIT) and rfc3986-validator0.1.1 (MIT).
Native uv produces the complete version/hash lock used by pip; the installed report is retained under artifacts/portability.
Primary metadata: https://pypi.org/project/jsonschema/ and https://pypi.org/project/PyYAML/.
These are development controls, excluded from npm payloads.

The existing operator's DCG remains outside this repository's adoption.
Its historical rights record includes a non-standard rider; retaining a source reference does not grant new rights or authorize installation.
See [adoption](adoption.md).
